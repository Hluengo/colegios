import { describe, it, expect } from 'vitest';
import * as mod from './../../../pages/AdminPanel';

describe('auto: ./../../../pages/AdminPanel', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
