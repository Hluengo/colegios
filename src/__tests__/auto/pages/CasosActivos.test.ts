import { describe, it, expect } from 'vitest';
import * as mod from './../../../pages/CasosActivos';

describe('auto: ./../../../pages/CasosActivos', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
