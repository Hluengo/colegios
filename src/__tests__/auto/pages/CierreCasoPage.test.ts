import { describe, it, expect } from 'vitest';
import * as mod from './../../../pages/CierreCasoPage';

describe('auto: ./../../../pages/CierreCasoPage', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
