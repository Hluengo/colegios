import { describe, it, expect } from 'vitest';
import * as mod from './../../../utils/validation.schemas';

describe('auto: ./../../../utils/validation.schemas', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
