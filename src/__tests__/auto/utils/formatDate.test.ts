import { describe, it, expect } from 'vitest';
import * as mod from './../../../utils/formatDate';

describe('auto: ./../../../utils/formatDate', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
