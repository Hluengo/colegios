import { describe, it, expect } from 'vitest';
import * as mod from './../../../hooks/useSeguimientos';

describe('auto: ./../../../hooks/useSeguimientos', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
