import { describe, it, expect } from 'vitest';
import * as mod from './../../../hooks/useMediaQuery';

describe('auto: ./../../../hooks/useMediaQuery', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
