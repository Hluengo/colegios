import { describe, it, expect } from 'vitest';
import * as mod from './../../../hooks/useActionTypes';

describe('auto: ./../../../hooks/useActionTypes', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
