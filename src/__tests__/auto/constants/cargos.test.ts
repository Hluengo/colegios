import { describe, it, expect } from 'vitest';
import * as mod from './../../../constants/cargos';

describe('auto: ./../../../constants/cargos', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
