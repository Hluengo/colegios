import { describe, it, expect } from 'vitest';
import * as mod from './../../../lib/sentry';

describe('auto: ./../../../lib/sentry', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
