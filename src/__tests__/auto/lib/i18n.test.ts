import { describe, it, expect } from 'vitest';
import * as mod from './../../../lib/i18n';

describe('auto: ./../../../lib/i18n', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
