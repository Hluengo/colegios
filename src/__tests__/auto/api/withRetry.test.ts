import { describe, it, expect } from 'vitest';
import * as mod from './../../../api/withRetry';

describe('auto: ./../../../api/withRetry', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
