import { describe, it, expect } from 'vitest';
import * as mod from './../../../api/estadisticas';

describe('auto: ./../../../api/estadisticas', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
