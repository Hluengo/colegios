import { describe, it, expect } from 'vitest';
import * as mod from './../../../api/tenantHelpers';

describe('auto: ./../../../api/tenantHelpers', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
