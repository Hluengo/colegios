import { describe, it, expect } from 'vitest';
import * as mod from './../../../api/supabaseClient';

describe('auto: ./../../../api/supabaseClient', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
