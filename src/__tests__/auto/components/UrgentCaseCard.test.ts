import { describe, it, expect } from 'vitest';
import * as mod from './../../../components/UrgentCaseCard';

describe('auto: ./../../../components/UrgentCaseCard', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
