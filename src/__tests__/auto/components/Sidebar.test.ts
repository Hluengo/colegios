import { describe, it, expect } from 'vitest';
import * as mod from './../../../components/Sidebar';

describe('auto: ./../../../components/Sidebar', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
