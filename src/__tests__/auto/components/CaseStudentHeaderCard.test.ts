import { describe, it, expect } from 'vitest';
import * as mod from './../../../components/CaseStudentHeaderCard';

describe('auto: ./../../../components/CaseStudentHeaderCard', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
