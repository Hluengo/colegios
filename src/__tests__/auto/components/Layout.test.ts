import { describe, it, expect } from 'vitest';
import * as mod from './../../../components/Layout';

describe('auto: ./../../../components/Layout', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
