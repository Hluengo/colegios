import { describe, it, expect } from 'vitest';
import * as mod from './../../../components/CaseDetailsCard';

describe('auto: ./../../../components/CaseDetailsCard', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
