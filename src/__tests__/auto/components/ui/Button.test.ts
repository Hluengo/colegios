import { describe, it, expect } from 'vitest';
import * as mod from './../../../../components/ui/Button';

describe('auto: ./../../../../components/ui/Button', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
