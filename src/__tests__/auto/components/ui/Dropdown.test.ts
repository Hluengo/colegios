import { describe, it, expect } from 'vitest';
import * as mod from './../../../../components/ui/Dropdown';

describe('auto: ./../../../../components/ui/Dropdown', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
