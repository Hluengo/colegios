import { describe, it, expect } from 'vitest';
import * as mod from './../../../../components/ui/Avatar';

describe('auto: ./../../../../components/ui/Avatar', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
