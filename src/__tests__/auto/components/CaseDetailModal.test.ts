import { describe, it, expect } from 'vitest';
import * as mod from './../../../components/CaseDetailModal';

describe('auto: ./../../../components/CaseDetailModal', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
