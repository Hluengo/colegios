import { describe, it, expect } from 'vitest';
import * as mod from './../../../components/Tooltip';

describe('auto: ./../../../components/Tooltip', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
