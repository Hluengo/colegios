import { describe, it, expect } from 'vitest';
import * as mod from './../../../components/SeguimientoForm';

describe('auto: ./../../../components/SeguimientoForm', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
