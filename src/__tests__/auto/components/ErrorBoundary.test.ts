import { describe, it, expect } from 'vitest';
import * as mod from './../../../components/ErrorBoundary';

describe('auto: ./../../../components/ErrorBoundary', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
