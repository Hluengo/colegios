import { describe, it, expect } from 'vitest';
import * as mod from './../../../components/VirtualizedCaseList';

describe('auto: ./../../../components/VirtualizedCaseList', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
