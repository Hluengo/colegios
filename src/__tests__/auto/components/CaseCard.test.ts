import { describe, it, expect } from 'vitest';
import * as mod from './../../../components/CaseCard';

describe('auto: ./../../../components/CaseCard', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
