import { describe, it, expect } from 'vitest';
import * as mod from './../../../components/LanguageSwitcher';

describe('auto: ./../../../components/LanguageSwitcher', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
