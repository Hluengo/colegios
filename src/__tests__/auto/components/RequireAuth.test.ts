import { describe, it, expect } from 'vitest';
import * as mod from './../../../components/RequireAuth';

describe('auto: ./../../../components/RequireAuth', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
