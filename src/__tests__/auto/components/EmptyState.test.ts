import { describe, it, expect } from 'vitest';
import * as mod from './../../../components/EmptyState';

describe('auto: ./../../../components/EmptyState', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
