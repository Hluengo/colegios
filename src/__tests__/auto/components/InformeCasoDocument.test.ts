import { describe, it, expect } from 'vitest';
import * as mod from './../../../components/InformeCasoDocument';

describe('auto: ./../../../components/InformeCasoDocument', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
