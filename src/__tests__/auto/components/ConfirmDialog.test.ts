import { describe, it, expect } from 'vitest';
import * as mod from './../../../components/ConfirmDialog';

describe('auto: ./../../../components/ConfirmDialog', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
