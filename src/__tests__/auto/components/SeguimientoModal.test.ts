import { describe, it, expect } from 'vitest';
import * as mod from './../../../components/SeguimientoModal';

describe('auto: ./../../../components/SeguimientoModal', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
