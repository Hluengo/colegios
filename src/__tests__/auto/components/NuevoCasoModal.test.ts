import { describe, it, expect } from 'vitest';
import * as mod from './../../../components/NuevoCasoModal';

describe('auto: ./../../../components/NuevoCasoModal', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
