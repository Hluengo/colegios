import { describe, it, expect } from 'vitest';
import * as mod from './../../../components/CaseDetailPanel';

describe('auto: ./../../../components/CaseDetailPanel', () => {
  it('exports something', () => {
    expect(mod).toBeDefined();
  });
});
