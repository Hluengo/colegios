import * as Mod from './SeguimientoItem';
const SeguimientoItem = (Mod as any).default || (Mod as any).SeguimientoItem || Mod;


describe('SeguimientoItem export', () => {
  it('is defined', () => {
    expect(Mod).toBeDefined();
  });
});
