import * as Mod from './DueProcessAccordion';
const DueProcessAccordion = (Mod as any).default || (Mod as any).DueProcessAccordion || Mod;


describe('DueProcessAccordion export', () => {
  it('is defined', () => {
    expect(Mod).toBeDefined();
  });
});
