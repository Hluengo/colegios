import * as Mod from './AccessibilitySettings';
const AccessibilitySettings = (Mod as any).default || (Mod as any).AccessibilitySettings || Mod;


describe('AccessibilitySettings export', () => {
  it('is defined', () => {
    expect(Mod).toBeDefined();
  });
});
