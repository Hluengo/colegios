import * as Mod from './EstadisticasDocument';
const EstadisticasDocument = (Mod as any).default || (Mod as any).EstadisticasDocument || Mod;


describe('EstadisticasDocument export', () => {
  it('is defined', () => {
    expect(Mod).toBeDefined();
  });
});
