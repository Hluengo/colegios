import * as Mod from './Dropdown';
const Dropdown = (Mod as any).default || (Mod as any).Dropdown || Mod;


describe('Dropdown export', () => {
  it('is defined', () => {
    expect(Mod).toBeDefined();
  });
});
