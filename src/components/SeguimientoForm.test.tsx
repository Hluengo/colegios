import * as Mod from './SeguimientoForm';
const SeguimientoForm = (Mod as any).default || (Mod as any).SeguimientoForm || Mod;


describe('SeguimientoForm export', () => {
  it('is defined', () => {
    expect(Mod).toBeDefined();
  });
});
