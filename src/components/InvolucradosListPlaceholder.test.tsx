import * as Mod from './InvolucradosListPlaceholder';
const InvolucradosListPlaceholder = (Mod as any).default || (Mod as any).InvolucradosListPlaceholder || Mod;


describe('InvolucradosListPlaceholder export', () => {
  it('is defined', () => {
    expect(Mod).toBeDefined();
  });
});
