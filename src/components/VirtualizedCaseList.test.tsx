import * as Mod from './VirtualizedCaseList';
const VirtualizedCaseList = (Mod as any).default || (Mod as any).VirtualizedCaseList || Mod;


describe('VirtualizedCaseList export', () => {
  it('is defined', () => {
    expect(Mod).toBeDefined();
  });
});
