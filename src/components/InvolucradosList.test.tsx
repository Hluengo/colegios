import * as Mod from './InvolucradosList';
const InvolucradosList = (Mod as any).default || (Mod as any).InvolucradosList || Mod;


describe('InvolucradosList export', () => {
  it('is defined', () => {
    expect(Mod).toBeDefined();
  });
});
