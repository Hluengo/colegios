import * as Mod from './SeguimientoModal';
const SeguimientoModal = (Mod as any).default || (Mod as any).SeguimientoModal || Mod;


describe('SeguimientoModal export', () => {
  it('is defined', () => {
    expect(Mod).toBeDefined();
  });
});
