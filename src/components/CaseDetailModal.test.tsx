import * as Mod from './CaseDetailModal';
const CaseDetailModal = (Mod as any).default || (Mod as any).CaseDetailModal || Mod;


describe('CaseDetailModal export', () => {
  it('is defined', () => {
    expect(Mod).toBeDefined();
  });
});
