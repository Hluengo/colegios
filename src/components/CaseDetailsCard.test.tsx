import * as Mod from './CaseDetailsCard';
const CaseDetailsCard = (Mod as any).default || (Mod as any).CaseDetailsCard || Mod;


describe('CaseDetailsCard export', () => {
  it('is defined', () => {
    expect(Mod).toBeDefined();
  });
});
