import * as Mod from './InvolucradoRow';
const InvolucradoRow = (Mod as any).default || (Mod as any).InvolucradoRow || Mod;


describe('InvolucradoRow export', () => {
  it('is defined', () => {
    expect(Mod).toBeDefined();
  });
});
