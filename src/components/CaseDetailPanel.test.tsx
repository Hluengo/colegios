import * as Mod from './CaseDetailPanel';
const CaseDetailPanel = (Mod as any).default || (Mod as any).CaseDetailPanel || Mod;


describe('CaseDetailPanel export', () => {
  it('is defined', () => {
    expect(Mod).toBeDefined();
  });
});
