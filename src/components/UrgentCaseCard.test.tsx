import * as Mod from './UrgentCaseCard';
const UrgentCaseCard = (Mod as any).default || (Mod as any).UrgentCaseCard || Mod;


describe('UrgentCaseCard export', () => {
  it('is defined', () => {
    expect(Mod).toBeDefined();
  });
});
