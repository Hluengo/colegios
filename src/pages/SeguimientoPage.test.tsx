import * as Mod from './SeguimientoPage';
const SeguimientoPage = (Mod as any).default || (Mod as any).SeguimientoPage || Mod;


describe('SeguimientoPage export', () => {
  it('is defined', () => {
    expect(Mod).toBeDefined();
  });
});
