import * as Mod from './CierreCasoPage';
const CierreCasoPage = (Mod as any).default || (Mod as any).CierreCasoPage || Mod;


describe('CierreCasoPage export', () => {
  it('is defined', () => {
    expect(Mod).toBeDefined();
  });
});
