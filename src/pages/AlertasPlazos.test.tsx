import * as Mod from './AlertasPlazos';
const AlertasPlazos = (Mod as any).default || (Mod as any).AlertasPlazos || Mod;


describe('AlertasPlazos export', () => {
  it('is defined', () => {
    expect(Mod).toBeDefined();
  });
});
