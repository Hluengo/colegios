# ============================================================================
# CONVIVENCIA ESCOLAR - Environment Configuration Template
# ============================================================================
# Copy this file to .env and configure values for your environment
# 
# Para desarrollo local: npm run dev
# Para producción: npm run build && npm run preview
# ============================================================================

# ============================================================================
# SUPABASE CONFIGURATION - Backend Database & Auth
# ============================================================================
# Get these values from: https://app.supabase.com -> Project Settings
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anonymous-key-here

# ============================================================================
# BRANDING CONFIGURATION - Institutional Customization
# ============================================================================
# Application-wide branding that can be customized per institution/environment
# These values are loaded in src/config/branding.ts

# Application display name (used in UI titles, headers)
# Default: "Convivencia Escolar"
# Example: "SGCE - Institución Educativa San José"
VITE_APP_NAME=Convivencia Escolar

# School/Institution name (used in documents, footers)
# Default: "Institución Educativa"
# Example: "Colegio Rural Las Flores"
VITE_SCHOOL_NAME=Institución Educativa

# Logo for main application header
# Must be a valid image URL (public/, relative, or absolute)
# Recommended: 200x80px, PNG with transparency
# Default: "/default-logo.png"
# Example: "/branding/logo-app.png"
VITE_LOGO_APP=/default-logo.png

# Logo for PDF documents (reports, case files, estadísticas)
# Must be a valid image URL (public/, relative, or absolute)
# Recommended: 800x320px, PNG with transparency
# Default: "/default-logo.png"
# Example: "/branding/logo-pdf.png"
VITE_LOGO_PDF=/default-logo.png

# Logo for Login/Authentication page
# Must be a valid image URL (public/, relative, or absolute)
# Recommended: 300x100px, PNG with transparency for auth panels
# Default: "/branding/logo-auth.png"
# Example: "/branding/praxia-novus-logo-auth.png"
VITE_LOGO_AUTH=/branding/logo-auth.png

# Fallback logo if VITE_LOGO_AUTH fails to load
# Used when main auth logo cannot be retrieved (network error, 404, etc)
# Must match an available image in public/ directory
# Default: "/default-logo.png"
# Critical: This path MUST exist to prevent broken UI
VITE_LOGO_AUTH_FALLBACK=/default-logo.png

# Logo for browser tab & bookmarks
# Must be a valid image URL (public/, relative, or absolute)
# Supported formats: .ico, .jpg, .png, .gif, .svg
# Default: "/veritas.jpg"
# Example: "/branding/favicon.png" (must exist in public/)
VITE_FAVICON=/branding/veritas.jpg

# ============================================================================
# API CONFIGURATION - External Services & Endpoints
# ============================================================================
# API Base URL for health checks and admin operations
# Default: Uses relative path (same-origin proxy)
# Production: May need to be set if using separate API domain
# VITE_API_BASE_URL=https://api.example.com

# ============================================================================
# FEATURE FLAGS - Enable/Disable Features
# ============================================================================
# Debug mode for development (shows logs, error details)
# Production: Should always be "false"
VITE_DEBUG_MODE=false

# ============================================================================
# BUILD CONFIGURATION - Vite Build Options
# ============================================================================
# These are auto-detected but can be overridden
# VITE_PORT=5173
# VITE_HOST=localhost

# ============================================================================
# NOTES FOR ADMINISTRATORS & DEPLOYMENT
# ============================================================================
# 
# MULTI-INSTITUTION SETUP:
# 1. Copy this file to .env.local per institution
# 2. Update VITE_APP_NAME and VITE_SCHOOL_NAME
# 3. Upload custom logos to public/branding/ (or CDN)
# 4. Update VITE_LOGO_* and VITE_FAVICON paths
# 5. Set VITE_LOGO_AUTH_FALLBACK to a safe default
# 6. Test build: npm run build
#
# BRANDING ASSETS LOCATION:
# - Copy logos to: public/branding/
# - Example structure:
#   public/
#   ├── branding/
#   │   ├── logo-app.png          (200x80px)
#   │   ├── logo-pdf.png          (800x320px)
#   │   ├── logo-auth.png         (300x100px)
#   │   └── favicon.png           (32x32px or larger)
#   └── default-logo.png          (universal fallback)
#
# PRODUCTION DEPLOYMENTS:
# - All VITE_LOGO_* paths must return HTTP 200
# - Test each image URL before deploying
# - Use CDN for logos if serving from multiple regions
# - Monitor console.warn for image load failures
#
# SECURITY CONSIDERATIONS:
# - VITE_SUPABASE_ANON_KEY is public (embedded in frontend)
# - Never put secrets in VITE_* variables
# - Row-Level Security (RLS) must be enforced in Supabase
# - All user input validated server-side
#
# TROUBLESHOOTING:
# - Favicon not updating: Clear browser cache (Ctrl+Shift+Del)
# - Logos not loading: Check CORS headers on image server
# - Images on mobile: Verify responsive CSS in Tailwind config
# - Build fails: Check all VITE_LOGO_* paths exist in public/
#
# For more info: See BRANDING_SETUP.md in project root
# ============================================================================
